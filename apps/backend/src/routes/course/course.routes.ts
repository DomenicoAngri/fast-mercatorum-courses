// import express from "express";
// import * as courseController from "../controllers/fastCourseController";

// const router = express.Router();

// // Get all courses
// router.get("/", courseController.getCourses);

// // Get details of a specific course
// router.get("/:id", courseController.getCourseById);

// // Start automatic viewing of a course
// router.post("/:id/start", courseController.startCourse);

// // Stop viewing a course
// router.post("/:id/stop", courseController.stopCourse);

// // Get current status of running courses
// router.get("/status", courseController.getCourseStatus);

// export default router;
// // 